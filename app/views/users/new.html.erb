<form action="<%= users_url %>" method="post">
  <%= auth_token %>

  <%= render "fields", user: @user %>

  <button>Sign up for Code Genius</button>
</form>
Already signed up? Go <a href="<%= new_session_url %>">here</a>.
